{
  "name": "marco-analytics",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute marco-analytics --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute marco-analytics --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
